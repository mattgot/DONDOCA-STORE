import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  drawerHeader: {
    alignItems: 'center',
    marginTop: 16,
    marginBottom: 24,
  },
  drawerLogo: {
    width: 60,
    height: 60,
    borderRadius: 12,
    marginBottom: 8,
  },
  drawerUser: {
    fontSize: 19,
    fontWeight: 'bold',
    color: 'white',
  },

 
});

export default styles;